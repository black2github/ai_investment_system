# Для передачи другой LLM: план этапа принят; заказ того, что не зависит от Flight 14

Вставляется одним сообщением от «=== НАЧАЛО ===» до «=== КОНЕЦ ===».

=== НАЧАЛО ===

## Принято 21.09.2026
- `SPCX_mpc_inputs_v1.0` развёрнут: 32 драйвера, 12 failure modes, общие причины со SPACE- и AI-кластерами. Значения
  16 новых драйверов совпали с вашим патчем v1.1. Теперь входы MPC полные у всех 14 компаний.
- Порядок этапа с вашей поправкой принят: спецификации Portfolio Optimizer v1.0 и Portfolio Stability Test v1.0 делаются
  параллельно с MC v1.1 и должны быть готовы до первого прогона MPC; калибровки RV/MC — по две компании в порядке
  NBIS+NVDA → HOOD+LLY → META+ASML → RKLB+MSFT → NET+PLTR → ETN+ASTS → CRWV; MPC-пилот на текущем портфеле — как только
  готовы NBIS, NVDA, HOOD; три архетипа MC-калибровки — до массовых калибровок; I1–I5 — через общий латентный фактор
  с неполно коррелированными шоками по горизонтам и узлам вместо одного ранга.

## Заказ сейчас — то, что не зависит от теста Flight 14 (23.09)
1. **Архетипы MC-калибровки v1.0** — спецификация трёх классов (`mature_positive_margin`, `capital_intensive_transition`,
   `pre_service_or_milestone_driven`) с указанием: форма траектории маржи, какие узлы и распределения обязательны,
   какие терминальные мультипликаторы и как выбираются (FCF / revenue bridge / milestone-condition), какие поля
   калибровочного YAML общие для всех классов, а какие специфичны. Плюс таблица «компания → архетип (модификация)» для 14
   бумаг. Это позволит мне заранее обобщить движок MC под классы, а не под одну компанию.
2. **Portfolio Optimizer v1.0** — спецификация и машинная схема: целевая функция (или её отсутствие в пользу ограничений),
   ограничения из Constitution §28/§30 и `_portfolio.yaml` (лимиты на бумагу, сектор, Challenger, неликвид, dry powder —
   пока пустые, нужны ваши рекомендуемые значения как `model_assumption` для утверждения владельцем), диапазон тестовых
   весов для MPC (`portfolio_optimizer_allowed_range`), правило учёта ограничений исполнения (лоты, опционный хедж от 100
   акций, два счёта), выходной формат (предлагаемые веса + ex-post сравнение с `initial_portfolio_hypothesis`).
3. **Portfolio Stability Test v1.0** — спецификация возмущений (доходности, корреляции, вероятности сценариев,
   терминальные допущения, исключение драйвера по одному), критерии «устойчиво / неустойчиво» для включения бумаги и её
   веса, выходной формат для MPC (`robustness: worsens/unstable`).

MC v1.1 и первую пару калибровок (NBIS + NVDA) — после 23.09, как договорились. Если удобнее выдать 1–3 партиями,
порядок: 1 → 2 → 3.

=== КОНЕЦ ===
