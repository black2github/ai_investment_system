# Для передачи IMMA (новый чат): исходные v1.0 для нормативного переиздания META/ASML v1.0.1

Черновик 24.09.2026, НЕ отправлен. Одно сообщение от «=== НАЧАЛО ===» до «=== КОНЕЦ ===» с вложениями из папки
Downloads/mc-meta-asml-v10-sources.

=== НАЧАЛО ===

Спасибо за подтверждение по LLY и за patch-plan. Исходные файлы во вложении — они не вошли в пакет восстановления, это
наш пропуск: META_mc_calibration_v1.0.yaml, ASML_mc_calibration_v1.0.yaml (те самые хост-входы), их RV-калибровки
(META_calibration_v1.0.yaml, ASML_calibration_v1.0.yaml — не менять, run_ref …-94a270 / …-2d7985) и для полноты
LLY_mc_calibration_v1.0.yaml (принят как есть).

Просьба выпустить нормативные полные META_mc_calibration_v1.0.1.yaml и ASML_mc_calibration_v1.0.1.yaml по вашей же
процедуре из patch notes: исходный файл + операции плана, пересчитанная локальная устойчивость v1.1.2, schema 1.0.2,
company_mc 2.3.1, PACKAGE_MANIFEST с sha256; мы проверим полноту замены check_supersedes и прогоним конвейер.

Два замечания к plan, чтобы переиздание прошло с первого раза:
1. Имена сегментов ASML в плане (EUV, DUV, InstalledBase) не совпадают с файлом: в нём EUV_Systems, DUV_Systems,
   InstalledBaseManagement и отдельный Metrology_Inspection (в плане не упомянут — решите, расширять ли его тоже).
2. Семантика `lower_tail_pct_of_mode: -0.20` для мультипликаторов читается двояко; мы трактовали как «min − 0.20·mode,
   max + 0.15·mode» (иначе для META Y5 17/25/34 нижняя граница стала бы уже). В переиздании достаточно самих чисел.

3. Два ограничения плана ASML сужают, а не расширяют хвост: у terminal_margin_Y8 min_floor 0.20 выше текущего min
   0.18, у InstalledBase(Management).initial_growth max_ceiling 0.30 ниже текущего max 0.32; terminal_margin_Y5 с
   floor 0.20 = текущему min нижний хвост не расширяется вовсе. Вероятно, floor/ceiling должны быть не строже текущих
   границ.
Ориентировочно (наш предпросмотр плана в памяти на движке 2.3.1, 20k путей, НЕ норматив): META intrinsic W
0.214 → ≈0.30 (ориентир B 0.40 не достигается — по вашему же правилу дальше не расширять, а документировать), ASML
0.214 → ≈0.26 (ориентир 0.25 достигнут). Схема и MC-G5-013 на патченных копиях проходят. Решение о достаточности —
ваше при переиздании.

=== КОНЕЦ ===

## Вложения (Downloads/mc-meta-asml-v10-sources)
- `META_mc_calibration_v1.0.yaml`, `ASML_mc_calibration_v1.0.yaml` — хост-входы приёмки (схема v1.0.2).
- `META_calibration_v1.0.yaml`, `ASML_calibration_v1.0.yaml` — RV-калибровки (приняты, не менять).
- `LLY_mc_calibration_v1.0.yaml` — принятая калибровка LLY (для полноты контекста).
